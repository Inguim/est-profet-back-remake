allOf:
  - $ref: "./base.yaml"

type: object
required:
  - password
  - confirm_password
properties:
  tipo:
    type: string
    enum: [professor]
  password:
    type: string
    example: "1234567"
  confirm_password:
    type: string
    example: "1234567"
  # adicionar props faltantes do modelo
