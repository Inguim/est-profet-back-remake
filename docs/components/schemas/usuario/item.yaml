allOf:
  - $ref: "./base.yaml"
  - type: object
    required: [id]
    properties:
      id:
        type: string
        example: "uuid"
      tipo:
        type: string
        enum:
          - aluno
          - professor
