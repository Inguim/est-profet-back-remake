get:
  tags: [Projetos]
  summary: Busca um Projeto por ID
  parameters:
    - name: id
      in: path
      required: true
      description: ID do Projeto
      schema:
        type: string
        example: "123"
  responses:
    "200":
      description: Projeto encontrado com sucesso
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: Projeto encontrado com sucesso
              data:
                $ref: "../../../components/schemas/projeto-completo/get.yaml"
    "404":
      $ref: "../../../components/responses/404_not_found.yaml"

patch:
  tags: [Projetos]
  summary: Atualiza um Projeto
  security:
    - BearerAuth: []
  parameters:
    - name: id
      in: path
      required: true
      description: ID do projeto
      schema:
        type: string
        example: "123"

  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/projeto/update.yaml"

  responses:
    "200":
      description: Projeto atualizado com sucesso
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: Projeto atualizado com sucesso
              data:
                $ref: "../../../components/schemas/projeto/item.yaml"
    "400":
      $ref: "../../../components/responses/400_validacao.yaml"
    "401":
      $ref: "../../../components/responses/401_usuario_sem_autorizacao.yaml"
    "404":
      $ref: "../../../components/responses/404_not_found.yaml"
