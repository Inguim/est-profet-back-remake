get:
  tags: [Menus]
  summary: Retorna uma lista de Menus
  responses:
    "200":
      description: "Retorna uma lista de Menus"
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: Menus buscados com sucesso
              data:
                $ref: "../../components/schemas/menu/collection.yaml"
